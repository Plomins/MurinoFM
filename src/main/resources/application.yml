spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/murinofm_db
    username: postgres
    password: ${DB_PASSWORD:default_password}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true